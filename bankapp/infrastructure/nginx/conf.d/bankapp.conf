upstream api_backend {
    server backend:3001;
    keepalive 32;
}

upstream spa_frontend {
    server frontend:80;
    keepalive 16;
}

server {
    listen 80;
    server_name _;

    # Security headers
    add_header X-Frame-Options        "SAMEORIGIN"    always;
    add_header X-Content-Type-Options "nosniff"       always;
    add_header X-XSS-Protection       "1; mode=block" always;
    add_header Referrer-Policy        "strict-origin-when-cross-origin" always;

    # Health check endpoint (pour Azure load balancer)
    location = /health {
        return 200 "OK\n";
        add_header Content-Type text/plain;
        access_log off;
    }

    # API health (sans log)
    location = /api/v1/health {
        limit_req zone=general burst=20 nodelay;
        proxy_pass http://api_backend;
        proxy_set_header Host            $host;
        proxy_set_header X-Real-IP       $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        access_log off;
    }

    # Auth routes : rate limit strict
    location ~ ^/api/v[0-9]+/auth/(login|register) {
        limit_req zone=auth burst=5 nodelay;
        proxy_pass         http://api_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host             $host;
        proxy_set_header   X-Real-IP        $remote_addr;
        proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 30s;
        add_header Cache-Control "no-store" always;
    }

    # Toutes les autres routes API
    location /api/ {
        limit_req zone=api burst=20 nodelay;
        proxy_pass         http://api_backend;
        proxy_http_version 1.1;
        proxy_set_header   Connection       "";
        proxy_set_header   Host             $host;
        proxy_set_header   X-Real-IP        $remote_addr;
        proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_connect_timeout 10s;
        proxy_read_timeout    30s;
        proxy_send_timeout    30s;
        add_header Cache-Control "no-store" always;
    }

    # Frontend SPA
    location / {
        limit_req zone=general burst=50 nodelay;
        proxy_pass         http://spa_frontend;
        proxy_http_version 1.1;
        proxy_set_header   Host            $host;
        proxy_set_header   X-Real-IP       $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# Exemple HTTPS (décommenter après ajout de certificats SSL dans /infrastructure/nginx/ssl/)
# server {
#     listen 443 ssl http2;
#     server_name votre-domaine.com;
#
#     ssl_certificate     /etc/nginx/ssl/cert.pem;
#     ssl_certificate_key /etc/nginx/ssl/key.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
#     ssl_session_cache   shared:SSL:10m;
#
#     add_header Strict-Transport-Security "max-age=63072000; includeSubDomains" always;
#     # ... (reprendre le bloc location ci-dessus)
# }
